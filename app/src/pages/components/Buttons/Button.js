import styled from 'styled-components';

const Button = styled.TouchableHighlight`
        top: 6%;
        width: 90%;
        height: 60px;
        position: absolute;
    `;

export default Button;